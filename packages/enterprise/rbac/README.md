# @foxschema-enterprise/rbac

Private enterprise stub. **Not shipped** in the community npm distribution.

Implements `RbacProvider` from `@foxschema/rbac-contract` in a later phase
(workspace/channel-aware grants). Community builds must not depend on this
package at runtime.
