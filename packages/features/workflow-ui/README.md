# @foxschema/feature-workflow-ui

Thin feature home for Workflow UI.

**Implementation today:** `apps/web/src/frontend/features/workflow/`

This package only exports `FEATURE_ID` so future shell / nav wiring can
reference a stable id without importing the web app.
