# FoxSchema web backend config. Copy to `.env` and fill in what you need.
# Loaded automatically by the backend (apps/web). NEVER commit your real .env.

# --- SSO (optional) ---------------------------------------------------------
# A provider's "Continue with…" button appears only when BOTH its id + secret
# are set. Register this redirect/callback URL with each provider:
#   <app-origin>/api/auth/sso/<provider>/callback
# e.g. http://localhost:5173/api/auth/sso/github/callback  (dev)
# Optionally pin the origin used to build that URL:
# SSO_REDIRECT_BASE=http://localhost:5173

# Google  — https://console.cloud.google.com/apis/credentials (OAuth client)
# SSO_GOOGLE_CLIENT_ID=
# SSO_GOOGLE_CLIENT_SECRET=

# Microsoft / Entra ID — https://entra.microsoft.com (App registrations)
# SSO_MICROSOFT_CLIENT_ID=
# SSO_MICROSOFT_CLIENT_SECRET=
# SSO_MICROSOFT_TENANT=common   # or your tenant id

# GitHub — https://github.com/settings/developers (OAuth Apps) — easiest to test
# SSO_GITHUB_CLIENT_ID=
# SSO_GITHUB_CLIENT_SECRET=

# --- Update check -----------------------------------------------------------
# Defaults to the public npm package feed (how Fox is published):
#   https://registry.npmjs.org/foxschema/latest
# Override with a custom JSON feed ({ "version", "url", "notes" }) or a GitHub
# releases/latest URL. Set UPDATE_FEED_URL=off to disable.
# UPDATE_FEED_URL=https://registry.npmjs.org/foxschema/latest
# APP_VERSION=0.2.4
# One-click "Update now" (npm install -g foxschema@latest). CLI `open` sets true.
# FOXSCHEMA_SELF_UPDATE=true

# --- Marketing webhooks (optional) ------------------------------------------
# First-open email subscriber → foxschema.com WordPress signup endpoint.
# SIGNUP_WEBHOOK_URL=https://foxschema.com/wp-json/foxschema/v1/signup
# SIGNUP_WEBHOOK_SECRET=
# Post-login onboarding survey (role / database / goal) for marketing personas.
# Falls back to SIGNUP_WEBHOOK_URL when unset (payload includes event=onboarding_survey).
# ONBOARDING_WEBHOOK_URL=https://foxschema.com/wp-json/foxschema/v1/onboarding
# ONBOARDING_WEBHOOK_SECRET=

# --- Encryption (dev only; ops sets this in prod) ---------------------------
# APP_ENCRYPTION_KEY=<64 hex chars>
