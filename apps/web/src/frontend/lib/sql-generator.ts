// Re-export from core browser entrypoint — single source of truth for SQL generation logic.
export { SqlGeneratorModule } from '@foxschema/sql';
export type { MigrationStep, SchemaMapping, SqlDialect } from '@foxschema/sql';
