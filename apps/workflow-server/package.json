{
  "name": "@foxschema/workflow-server",
  "version": "0.1.0",
  "description": "Workflow engine HTTP process. Reached only through FoxSchema's permission-checked engine proxy.",
  "type": "module",
  "license": "Apache-2.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tedious-code/foxschema.git",
    "directory": "apps/workflow-server"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "start:scheduler": "tsx src/scheduler.ts",
    "start:worker": "tsx src/worker.ts"
  },
  "dependencies": {
    "@fastify/rate-limit": "11.1.0",
    "@foxschema/workflow-engine": "*",
    "fastify": "5.12.4",
    "fastify-raw-body": "5.0.0",
    "tsx": "4.23.13",
    "zod": "4.4.3"
  }
}
